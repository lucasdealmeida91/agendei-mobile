import { COLORS, FONT_SIZE } from "../../constants/theme"
export const styles = {
    btn: {
        backgroundColor: COLORS.blue, padding: 12, borderRadius: 6, width: "100%"
    },
    text: {
        color: COLORS.gray5, fontSize: FONT_SIZE.md, textAlign: "center",
    },
    primary: {
        backgroundColor: COLORS.blue,
    },
    danger: {
        backgroundColor: COLORS.red
    }

}


