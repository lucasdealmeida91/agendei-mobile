import { COLORS, FONT_SIZE } from "../../constants/theme"
export const styles = {
    container: { padding: 20, backgroundColor: COLORS.white, flex: 1,  },
    text: { fontSize: FONT_SIZE.md, color: COLORS.gray1, marginLeft: 15,marginBottom: 15 } ,
}

